apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: minio
spec:
  dnsNames:
    - s3.mayongcong.net
    - oss.mayongcong.net
    - s3-api.mayongcong.net
    - oss-api.mayongcong.net
  secretName: minio
  issuerRef:
    name: ca # Use the issuer created previously
    kind: ClusterIssuer